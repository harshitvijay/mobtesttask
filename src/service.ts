// import { StateStore } from "./Mobx/store";
export const fetchUsers = async (url: string) => {
  try {
    const response = await fetch(url);
    const data = await response.json();
    return data;
  } catch (error) {
    console.log(error);
  }
};
